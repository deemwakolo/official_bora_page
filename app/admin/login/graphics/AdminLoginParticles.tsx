export default function AdminLoginParticles() {
  return (
    <div
      className="absolute inset-0 opacity-[0.35]"
      style={{
        backgroundImage:
          'radial-gradient(color-mix(in srgb, var(--bora-gold) 30%, transparent) 1px, transparent 1px)',
        backgroundSize: '42px 42px',
      }}
    />
  );
}
